{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass-watch": "sass --watch ./src/styles:./dist/styles",
    "tsc-watch": "tsc -w",
    "lite-server": "lite-server",
    "start": "npm-run-all -p sass-watch tsc-watch lite-server"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.5.4",
    "npm-run-all": "^4.1.5"
  }
}